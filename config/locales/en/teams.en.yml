en:
  teams:
    fields:
      _:
      subdomain:
        _: &subdomain Subdomain
        label: "Syncro Subdomain"
        heading: "Syncro Subdomain"
        help: "Enter your syncro subdomain"
      api:
        _: &api Api
        label: "Syncro API Key"
        heading: "Syncro API Key"
        help: "An API KEY with full access is required"
      allowed_updates:
        _: &allowed_updates Allowed Updates
        label: "Are we allowed to update live from the Syncro API?"
        heading: *allowed_updates
        options:
          yes: "Yes - Use Live Data"
          no: "No - Use Offline Data Only"
      # 🚅 super scaffolding will insert new fields above this line.
  activerecord:
    attributes:
      membership:
        _:
        subdomain: *subdomain
        api: *api
        allowed_updates: *allowed_updates
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
